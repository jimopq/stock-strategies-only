"""靜態儀表板產生器。

排程跑完 main.py 後，讀當日訊號 + parquet 快取裡的價格資料，
產生純靜態 HTML 推到 GitHub Pages。

刻意不用 web/ 的 Next.js：
- 這裡的內容是「把算好的資料排版出來」，不需要前端框架
- 靜態產生 = CI 不必 npm install、不必維護前端依賴
- web/ 的策略管理需要即時 API，本來就無法靜態化，兩者關注點不同

產生的頁面不含任何憑證，只有計算結果。
"""
